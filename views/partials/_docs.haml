- unless @documents.nil?
  - @documents.each do |doc|
    %li(role='presentation') 
      %a.file(class='file-list' href="claims/#{doc[:file]}" name="#{doc[:name]}" role='menuitem' tabindex='-1')
        %i.icon-file.a
        = doc[:name]
